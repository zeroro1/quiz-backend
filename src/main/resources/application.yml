# Server configuration
server:
  port: 8080

# Database configuration
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/quiz_db?useSSL=false&serverTimezone=Asia/Shanghai&characterEncoding=utf8&connectionCollation=utf8mb4_unicode_ci&allowPublicKeyRetrieval=true
    username: root
    password: Gzh1994429
    driver-class-name: com.mysql.cj.jdbc.Driver

# MyBatis-Plus configuration
mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    db-config:
      id-type: auto

# WeChat Mini Program configuration
wx:
  app-id: YOUR_WX_APP_ID
  app-secret: YOUR_WX_APP_SECRET

# Agnes AI API configuration
agnes:
  ai:
    api-key: YOUR_AGNES_API_KEY
    api-url: https://api.agnes-ai.com/v1/chat/completions
    model: Agnes-2.0-Flash
    quiz-mode: AI_ONLY
